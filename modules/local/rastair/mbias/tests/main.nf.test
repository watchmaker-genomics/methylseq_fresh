
nextflow_process {

    name "Test Process RASTAIR_MBIAS"
    script "../main.nf"
    process "RASTAIR_MBIAS"

    tag "rastair"
    tag "rastair/mbias"

    test("Rastair MBias conversion calls") {

        when {
            process {
                """
                input[0] = Channel.of([
                    [ id:'test' ], // meta map
				    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.methylated.sorted.bam', checkIfExists: true)
                ])
                input[1] = Channel.of([
                    [ id:'test' ], // meta map
				    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.methylated.sorted.bam.bai', checkIfExists: true)
                ])
                input[2] = Channel.of([
                    [ id:'test' ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                ])
                input[3] = Channel.of([
                    [ id:'test' ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta.fai', checkIfExists: true)
                ])
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                // I need to define the snapshot comparison
                // { assert snapshot(
                //     process.out.txt,
                //     process.out.versions
                //     ).match()
                // }
            )
        }

    }

}
